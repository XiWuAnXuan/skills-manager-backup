interface:
  display_name: "Codex Windows Fast Patch"
  short_description: "Repatch Windows Codex Desktop gates and local plugins."
  brand_color: "#2563EB"
  default_prompt: "Use $codex-windows-fast-patch to inspect and repair Codex Desktop Fast Mode, locale, Chrome/browser_use, plugin, and Computer Use gates on Windows."

policy:
  allow_implicit_invocation: true
